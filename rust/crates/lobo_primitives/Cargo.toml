[package]
name = "lobo_primitives"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Price representations, quantities, timestamps, and identifiers for LOBO"

[lints]
workspace = true

[dependencies]
uuid = { version = "1.24.0", features = ["serde", "v4"] }
pyo3 = { version = "0.29.0", optional = true }
chrono = "0.4.45"
serde = { workspace = true, optional = true }


[features]
default = []
python = ["dep:pyo3"]
serde = ["dep:serde"]
# python = []
